const edadEsValida =
  edadIngresada !== null &&
  edadIngresada.trim() !== "" &&
  !isNaN(edadUsuario);
 
if (!edadEsValida) {
  console.error("Error: Por favor, ingresa una edad válida en números.");
} else {
