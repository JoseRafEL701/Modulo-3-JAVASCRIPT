const edadEsValida =
  edadIngresada !== null &&
  edadIngresada.trim() !== "" &&
  !isNaN(edadUsuario);
 
  if (edadUsuario < 18) {
    // Caso: el usuario es menor de edad
    const mensajeMenorEdad = `Hola ${nombreUsuario}, eres menor de edad. ¡Sigue aprendiendo y disfrutando del código!`;
    alert(mensajeMenorEdad);
    console.log(mensajeMenorEdad);
  } else {
    // Caso: el usuario es mayor o igual de edad
    const mensajeMayorEdad = `Hola ${nombreUsuario}, eres mayor de edad. ¡Prepárate para grandes oportunidades en el mundo de la programación!`;
    alert(mensajeMayorEdad);
    console.log(mensajeMayorEdad);
  }
}
