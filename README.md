# Modulo-3-JAVASCRIPT
### Este repositorio, tiene en cuenta lo que hemos dado en javascript los metodos en La implementación de bucles (for...in, for...of) y métodos (forEach, Object.keys(), Object.values(), Object.entries()).
### Los fundamentos SPA, manejo,manipulación de variables (let, const) y estructuras de datos (objetos, arrays, sets, maps).
